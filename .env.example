# 复制为 .env.local（已在 .gitignore，仅本地生效）
# pnpm dev 时会自动加载：.env → .env.local → .env.development → .env.development.local

# 前端 axios baseURL；开发留空可走 Vite 代理 /api
VITE_API_BASE_URL=

# Vite dev 代理目标（vite.config.ts 中 /api 转发）
VITE_API_PROXY_TARGET=http://localhost:8080
